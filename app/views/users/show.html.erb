<h2><%= @user.name %>投稿一覧</h2>
  <div class="album py-5">
    <div class="row">
      <%= render @cordinates %>
    </div>
  </div>
<h2><%= @user.name %>お気に入りコーディネート</h2>
  <div class="album py-5">
    <div class="row">
      <%= render @likes %>
    </div>
  </div>
  <p class="text-right mt-0">
      <%= link_to "お気に入り一覧", likes_user_path(current_user)  %>
  </p>  

